git init #Start git repo
git status #get status of your repo
git add <filename> #start tracking of file
git diff #To see what has been changed but not yet staged
git diff --staged #compares your staged changes to your last commit
git rm <filename> #Remove file from tracking and deleted from hard-drive
git rm --cached <filename> #Remove file from tracking but keep in hard-drive
